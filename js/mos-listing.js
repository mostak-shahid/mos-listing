jQuery(document).ready(function($) {
    /*$("button#mos_xml_upload_submit").live("click", function (event) {
        event.preventDefault();
        $('#processBtn').show();
        var action = $("#action").val();
        var file = $("#file").prop("files")[0];
        console.log(file);
        $.ajax({
            type : "post",
            dataType : "json",
            url : ajax_link,
            data : {
                action: action, 
                from: 'ajax_call',
                file : file, 
            },
            success: function(data) {
                console.log(data);
            }
        })        
    })*/
})